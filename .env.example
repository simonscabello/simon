# Banco de dados (PostgreSQL via Docker Compose)
DB_HOST=localhost
DB_PORT=5432
DB_USER=simon
DB_PASSWORD=change_me
DB_NAME=simon

# API (docker compose)
API_PORT=8080
JWT_SECRET=change_me_in_production
